import { renderTick } from '../components/renderStructure/renderTick';

export default {
  title: 'MatchUps/Tick',
  tags: ['autodocs'],
  render: ({ ...args }) => {
    return renderTick({ ...args });
  }
};

export const Tick = {
  args: {}
};
