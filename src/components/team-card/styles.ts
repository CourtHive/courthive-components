/**
 * Team Card — Class-name constants.
 *
 * One class per zone. CSS lives in `team-card.css` and is themed via
 * `--chc-*` custom properties so consumers can re-skin without touching
 * the primitive.
 */

export const tcCardClass = () => 'chc-team-card';
export const tcCardClickableClass = () => 'chc-team-card--clickable';
export const tcTitleRowClass = () => 'chc-team-card__title-row';
export const tcNameClass = () => 'chc-team-card__name';
export const tcNicknameClass = () => 'chc-team-card__nickname';
export const tcCountsClass = () => 'chc-team-card__counts';
