import { createTheme } from './createTheme';

export const typtiTheme = createTheme('typti-theme', {
  colors: {
    border: '#ff6a00',
    borderHover: '#ff6a00',
    connector: '#ff6a00',
    score: '#ff6a00',
    roundHeader: '#ff6a00',
    matchUp: 'var(--chc-bg-elevated)'
  }
});
